%h1 this is the show view

%h2= @book.title
%h4 Category: #{@book.category.name}
%h3= @book.author
%p= @book.description

= link_to 'Back', root_path
= link_to 'Edit', edit_book_path(@book)
= link_to 'Delete', book_path(@book), method: :delete,
  data: {confirm: 'Are you sure?'}
